kotlin.code.style=official
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
org.gradle.caching=true

# Publishing (manual, no CI Runner). Set the path to a local checkout of the dedicated static Maven
# repo (served at https://maven.luxgroup.dev) in ~/.gradle/gradle.properties or via -P:
#   kotrace.maven.repo.dir=/absolute/path/to/luxgroup-maven-checkout
# then: ./gradlew publishAllPublicationsToLuxGroupMavenRepository  &&  commit + push that repo.
